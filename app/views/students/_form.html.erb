<!-- app/views/students/_form.html.erb -->

<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_name %>
    <%= form.text_field :student_name %>
  </div>

  <div class="field">
    <%= form.label :student_age %>
    <%= form.number_field :student_age %>
  </div>

  <div class="field">
    <%= form.label :student_grade %>
    <%= form.text_field :student_grade %>
  </div>

  <div class="actions">
    <%= form.submit 'Save Student' %>
  </div>
<% end %>

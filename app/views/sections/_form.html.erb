<!-- app/views/sections/_form.html.erb -->

<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_name %>
    <%= form.text_field :section_name %>
  </div>

  <div class="field">
    <%= form.label :section_description %>
    <%= form.text_area :section_description %>
  </div>

  <div class="actions">
    <%= form.submit 'Save Section' %>
  </div>
<% end %>
